# Progress log — update after every significant work session

## Project overview
Signal Digest — see PRODUCT.md for full reasoning. Smart watchlist, per-instrument statistical anomaly flagging, ETag-pattern "since you last checked" state machine, live fault-injection demo.

## Canonical demo script (Stage 5)

This is the exact sequence every live rehearsal has followed (Sep 4 rehearsal entry below) and what `frontend/e2e/demo-rehearsal.spec.ts` automates end-to-end. Written down here as the single source of truth — PRODUCT.md's "Demo differentiation" section states *why* each beat matters; this is the literal *order and mechanics*.

1. **Fresh load.** Reset demo state (`backend/scripts/reset_demo_state.py`), load the dashboard. Bootstrap creates/discovers the demo watchlist, freshness banner shows `LIVE`, the digest renders real flags computed by the live scheduler from real seeded historical data (never fabricated).
2. **"Show your work."** Click a flagged row to open the evidence panel — the real mean±stdev band and historical points plotted via `GET /tickers/{ticker}/evidence`, with the flagged day's own point marked outside the band. Proves auditable statistics, not a black box.
3. **Acknowledge.** After the dwell delay, click Acknowledge on a flag — it disappears from the unacked view immediately (optimistic refetch, not a full reload).
4. **Fault injection.** Click Outage — freshness flips to `UNAVAILABLE` (immediate, not threshold-based). Click Recover — freshness returns to `LIVE` once the next real scheduler tick actually re-fetches (not snapped instantly).
5. **Combined beat: escalation-flip.** Freeze the clock (`stale`), run `seed_demo_escalation_precondition.py` for a real, not-yet-reached (ticker, trading_day) — the script independently pre-verifies, via the unmodified scoring functions, what that day's true severity will be, and seeds a placeholder at a deliberately-lower severity, acked. Recover, then wait for the live scheduler to naturally reach that day (no manual trigger). The flag reappears **unacknowledged**, corrected to the real engine-computed severity — driven by the actual `severity_rank` comparison in `upsert_flag_with_ack_bust`, not scripted.
6. **Today's Brief reflects it.** The one-line deterministic summary above the digest updates to name the now-escalated ticker and (when it crossed into `extreme`) the "including one extreme-severity move" clause — proving the brief tracks real committed state, not a snapshot.

**Demo time discipline (PRODUCT.md, unchanged):** the actual 5-minute stage demo is steps 1–2 and step 5 only; steps 3–4 and every other RELIABILITY.md scenario are Q&A material, not live narration. The E2E test below automates the full script (1–6) regardless, since its purpose is demo-*readiness* verification, not a script for the stage clock.

## Current state
- [ ] Planning complete: Stages 1-4 done (product direction, scoring engine, state machine, architecture/schema, reliability review) — see PRODUCT.md, ARCHITECTURE.md, DATA_MODEL.md, RELIABILITY.md
- [ ] Stage 5 (frontend UX for 9 screens, exact demo script) — pending
- [ ] Stage 6 (feature prioritization, 72h execution plan, testing strategy, repo structure) — pending
- [x] Repo scaffolded (Phase 0: backend FastAPI skeleton, frontend Next.js skeleton, docker-compose for Postgres)
- [x] Backend: data provider abstraction — `HistoricalReplayProvider` done (`backend/app/providers/`); `FaultInjectingProvider` still pending (later phase)
- [x] Backend: schema migrated (including severity_rank fix) — `backend/migrations/001_init.sql`, applied and verified locally
- [x] Backend: fixed ticker universe + sector mapping (`backend/app/data/tickers.py`, 35 tickers, hand-curated, `.NS`-suffixed consistently everywhere)
- [x] Backend: historical backfill script (`backend/scripts/seed_historical_data.py`) — run successfully against real Yahoo Finance data, verified in Postgres (see Sep 4 entry below)
- [x] Backend: baseline computation (`backend/app/data/baselines.py`) — mean/stdev return, volume avg, sample_size gating, tested
- [x] Backend: scoring engine (z-score, volume ratio, sector tag, volatility_regime) — `backend/app/services/scoring.py`, deterministic, no ML/composite score, tested and run against real seeded data
- [x] Backend: severity-escalation ack-bust transaction — `backend/app/services/flags.py`, implemented exactly per DATA_MODEL.md, tested against real Postgres (escalation busts ack, de-escalation doesn't, equal-rank rerun is a no-op)
- [x] Backend: digest API + ETag/ack flow — `GET /watchlists/{id}/digest`, `POST /watchlists/{id}/ack` (`backend/app/routers/watchlists.py`, `backend/app/services/digest.py`), tested against real Postgres including 304 semantics
- [x] Backend: evidence endpoint ("show your work") — `GET /tickers/{ticker}/evidence` (`backend/app/routers/tickers.py`, `backend/app/services/evidence.py`), real `price_ticks` only
- [x] Backend: demo-user identity seam — `backend/app/auth.py`, single hardcoded UUID, the only place "who is the current user" is decided (see architecture note below)
- [x] Backend: `/admin/fault`, DEMO_MODE-gated — `backend/app/routers/admin.py`, `FaultInjectingProvider` decorator (`backend/app/providers/fault_injecting.py`)
- [x] Backend: `GET /provider/status` — `backend/app/routers/provider_status.py`, real wall-clock `age_seconds`, never cached
- [x] Backend: minimal APScheduler background job — `backend/app/main.py` lifespan, calls `run_scoring_cycle` (`backend/app/services/scoring_pipeline.py`) on a configurable interval; `run_scoring_once.py` refactored to call the same function, no duplicated scoring logic
- [x] `GET /watchlists/{id}/digest`'s `freshness` field now reflects real `ProviderStatus.state`, not the Phase 3 hardcoded `"LIVE"`
- [x] Backend: `GET /watchlists` (ownership-scoped list) and `GET /tickers` (full universe) — `backend/app/routers/watchlists.py`, `backend/app/routers/tickers.py`
- [x] Backend: `demo_mode` field on `/provider/status` — frontend gates the fault-injection control on this, never guesses/hardcodes
- [x] Backend: `backend/scripts/reset_demo_state.py` — truncates accumulated demo scoring state, leaves real seeded data/user watchlists untouched
- [x] Frontend: digest view — `frontend/app/page.tsx`, MUST scope complete (bootstrap, ETag/304 polling, severity badges, deterministic explanations, ack-dwell, "new updates" affordance)
- [x] Frontend: evidence/distribution chart — `frontend/components/EvidencePanel.tsx`, hand-rolled inline SVG
- [x] Frontend: freshness banner + fault-injection demo toggle — `frontend/components/FreshnessBanner.tsx`, `frontend/components/FaultControl.tsx`, verified live via real browser automation (not curl)
- [x] Frontend SHOULD/NICE items: watchlist add/remove typeahead (Phase 7a) and historical changes log (Workstream 4) both built — see Workstream 4 entry below
- [x] Baseline data pulled (yfinance, 34/35 NSE tickers, history computed)
- [x] Demo script rehearsed — full live browser rehearsal of the exact PRODUCT.md sequence (bootstrap → digest → evidence → ack → fault injection → escalation-flip via `seed_demo_escalation_precondition.py` → recovery), see entry below
- [x] Digest scoped to `price_zscore` only, ranked by `|z_score|` descending — `volatility_regime` deliberately not surfaced (see PRODUCT.md)
- [x] Reliability scenarios #9, #10, #11, #12 test-verified against real Postgres (#10 revealed and fixed a real bug; #9/#11/#12 confirmed already correct)
- [x] README written (thesis, ETag precedent citation, trade-offs, limitations)
- [x] 100-word pitch written
- [x] Backend: rolling, look-ahead-safe baseline recomputation (Workstream 1) — `backend/app/data/baselines.py::compute_baseline_as_of`/`load_baseline_as_of`, wired into `scoring_pipeline.py` and `evidence.py`, one `baselines` row per `(ticker, as_of_date)` inserted per scheduler cycle, not a single static row per ticker
- [x] In-product brand identity (Workstream 4A) — `frontend/components/AppHeader.tsx` (wordmark + PRODUCT.md's own tagline), `AboutPanel.tsx` (accessible dialog, content sourced from PRODUCT.md)
- [x] Historical changes log (Workstream 4B) — `GET /tickers/{ticker}/flags` (`backend/app/routers/tickers.py`), `frontend/components/HistoryPanel.tsx`, reached via "View history" per ticker in `WatchlistManager`
- [x] Optional `LiveDelayedNSEProvider` (Workstream 3) — `backend/app/providers/live_delayed_nse.py`, gated behind `LIVE_PROVIDER_ENABLED` (default `false`), reachable only via `GET /provider/live-status`, status-only (not wired into scoring) — see entry below
- [x] "Today's Brief" (deterministic, no LLM) — `backend/app/services/brief.py`, `brief` field on `GET /digest`, `frontend/components/DigestBrief.tsx` — see entry below
- [x] Automated E2E demo rehearsal (Playwright) — `frontend/e2e/demo-rehearsal.spec.ts`, `npm run test:e2e`, walks the full canonical demo script (above) against a real backend/Postgres/frontend, no mocks — see entry below

## Next steps (update this first, every session)
1. **Phase 5 (frontend), the two live-review fixes, and Phase 6's reliability coverage pass are all complete.** Decide whether to retry `TATAMOTORS.NS` (currently absent — Yahoo 404) via a re-run of the seed script (safe/idempotent) or accept 34/35 for the demo universe.
2. ~~SHOULD/NICE frontend items deferred~~ Watchlist add/remove and the historical changes log are both built (Phase 7a, Workstream 4). Remaining minor gap: a dedicated empty/loading/error state pass beyond what's already built (digest skeleton, evidence panel, and History panel loading/error/empty states ARE done).
3. Before the live pitch demo: rehearse the exact escalation-flip beat using `backend/scripts/seed_demo_escalation_precondition.py` against a real, not-yet-reached (ticker, trading_day) — pick one fresh each rehearsal (the live rehearsal found the scheduler advances fast, ~5s/step continuously — freeze within the first ~10s of a fresh start if same-ticker narrative fidelity to the acked flag matters). Run `backend/scripts/reset_demo_state.py` first for a clean slate.
4. ~~Baselines are still static~~ **Resolved by Workstream 1 (2026-09-05, see entry below): baselines now roll forward per trading day, look-ahead-safe.** No look-ahead leakage remains in `price_zscore` scoring.
5. ~~Known UX gap: evidence panel backdrop intercepts all clicks, can't ack while it's open~~ **Resolved as a side effect of the UI Reference Adoption workstream (2026-09-05, see entry below):** the restyled EvidencePanel (now a drawer) has its own real Acknowledge action in its footer, so a flag can be acked directly from evidence without closing it first.
6. Remaining reliability scenarios not yet test-verified: #4, #5, #7, #8, #13, #14 — each already has adequate design coverage in RELIABILITY.md and/or earlier-phase tests; revisit only if time allows.

## Architecture notes carried forward past Phase 6

**`GET /digest` shows `price_zscore` only, ranked by `|z_score|` descending — a product decision, not a bug fix.** `app/services/digest.py`'s query now filters `signal_type = 'price_zscore'` and adds `ORDER BY abs(f.z_score) DESC`. `volatility_regime` flags are still computed and persisted by the scoring engine exactly as before (nothing in `scoring.py` changed) — live testing showed they were repetitive noise (same handful of tickers, same generic text, every cycle) because baselines were static at the time; see PRODUCT.md's dedicated note. **Update (Workstream 1, 2026-09-05): baselines are no longer static — see below — and the repetitiveness that originally motivated this filter is confirmed gone. The filter itself is unchanged; re-surfacing is a separate decision, not made in this workstream.**

**`trading_day_from_tick` had a real timezone bug, found by a reliability test before it shipped as a live one (RELIABILITY.md #10).** It computed `.date()` directly on the UTC-aware tick timestamp — the correct Asia/Kolkata trading day only coincided with the UTC calendar date when a tick fell well inside the IST day (true for real seeded data, anchored at 15:30 IST = 10:00 UTC, which is why this was never noticed in three phases of live testing). A tick near the UTC/IST midnight boundary would have silently bucketed into the wrong `trading_day`. Fixed: `tick.timestamp.astimezone(IST).date()` (`app/services/scoring.py`). Two regression tests added, one exercising exactly that boundary (19:30 UTC = 01:00 IST next day).

**Live demo rehearsal (2026-09-04) ran the complete PRODUCT.md sequence in one continuous flow via real browser automation** — bootstrap, digest polling, opening evidence, background-update buffering, fault injection (Live→Delayed→Stale→Unavailable→Recover→Live, all through real UI clicks and real polling), and the escalation-flip beat via `seed_demo_escalation_precondition.py` (seeded RELIANCE.NS/2025-10-20 at an artificial `notable`, script pre-verified the true outcome as `significant` z=2.909, the live scheduler corrected it exactly to that value and busted the ack — confirmed via DB and in the rendered digest). One real finding: the evidence panel's full-viewport backdrop intercepts all clicks, so acking a flag while its evidence panel stays open isn't actually possible in the current build (verified: attempting it just closes the panel, no accidental/wrong ack occurs). Confirmed via `PRODUCT.md`'s own demo-scope text that watchlist add/remove/second-watchlist is NOT required by the documented demo — the deferred SHOULD/NICE frontend work is genuinely out of scope for the pitch.

**Reliability scenarios #9, #10, #11, #12 are now test-verified against real Postgres, not just designed:**
- **#9 (partial ingestion)** — confirmed already correct by construction: each ticker's flag upsert is its own committed transaction inside `run_scoring_cycle`'s loop, so a mid-batch exception (simulated via a monkeypatched `load_latest_baseline` raising on the second ticker) leaves the first ticker's already-committed flags untouched, and a retry completes the remainder without duplicating what already succeeded (same row ids, not new ones).
- **#10 (timezone)** — real bug found and fixed, see above.
- **#11 (sector gating)** — confirmed already correct: `run_scoring_cycle` naturally gates `sector_relative` to `NULL` when fewer than 2 other sector members have a tick in that cycle, verified using a real `TICKER_SECTORS` sector ("Auto") through the full pipeline, not just the pure `sector_relative_tag()` unit test that already existed.
- **#12 (stale If-None-Match)** — confirmed already correct: a client presenting an ETag from several server-state-changes ago (multiple acks + a new flag in between) gets a full 200 with current data, never a false 304; a garbage/malformed ETag value also just gets a 200, not an error.

**Frontend ETag/polling state is managed as a pure reducer, separate from React state plumbing.** `frontend/lib/digestPoll.ts::applyDigestPollResult()` takes the previous `{data, etag}` and a poll result; a 304 returns the exact same object reference (state provably untouched), a 200 returns fresh data + the new ETag (falling back to the previous ETag if the header is somehow missing). `frontend/hooks/useDigest.ts` wraps this in a 5s-interval poller (matches the backend's default scheduler cadence — flags can't appear faster than that). The page keeps a separate `viewed` snapshot from `latest`: a background poll revealing new flags never silently rewrites what's on screen — `newFlagIds()` computes a "new since last viewed" count, shown as a pull-in affordance, confirmed live (saw "13 new updates" render correctly after a reload landed behind the live scheduler). After an ack, the page calls `pollOnce()` directly and sets `viewed` from its result immediately — bypassing the buffer, since that update was user-initiated, not a background surprise.

**Ack has a dwell delay before it's clickable, per Stage 2's design.** `frontend/lib/dwell.ts::scheduleDwellAck()` (1.5s, `ACK_DWELL_MS`) is a plain `setTimeout` wrapper, independent of any React hook, so it's directly unit-testable with fake timers. `DigestRow` resets and reschedules this on every `flag.id` change and disables the Ack button until it fires — verified live (clicking Ack after the dwell elapsed correctly wrote `flag_ack` and removed the row from view).

**Severity/freshness badges always render color + icon + text label together, enforced structurally, not by convention.** Both `SeverityBadge` and `FreshnessBanner` render through one shared `Badge` component (`frontend/components/Badge.tsx`) reading from `SEVERITY_CONFIG`/`FRESHNESS_CONFIG` (`frontend/lib/severity.ts`) — there is no code path that renders a color swatch without also rendering that config's icon and label, and a test asserts every config entry has non-empty icon+label+color and that icons are distinct per level (so screen readers/colorblind users never lose information color alone would have carried).

**Evidence chart is hand-rolled inline SVG, no charting library** (`frontend/components/EvidencePanel.tsx`) — a shaded rect for the mean±stdev band, circles for the real historical points, and the flagged day plotted separately with a dashed visual gap and its own date label, honestly reflecting that it may not be temporally contiguous with the plotted window (the baseline is static, per the Phase 3 evidence correction). Verified live: the band, points, and flagged point (in red, outside the band) all rendered correctly for a real flag.

**`GET /provider/status` now includes `demo_mode`** so the frontend knows whether to render the fault-injection control without guessing/hardcoding — reads the same `settings.demo_mode` flag `/admin/fault` itself is gated behind.

**Bootstrap watchlist creation is idempotent by construction, not by a client-side "already ran" flag alone.** `frontend/lib/bootstrap.ts::ensureBootstrapWatchlist()` always calls `GET /watchlists` first and only creates+seeds a new one if that list is empty — verified live: reloading the page after the first bootstrap left the watchlist count at exactly 1 in Postgres. A `useRef` guard in `page.tsx` additionally prevents React StrictMode's dev-mode double-effect-invocation from racing two concurrent bootstrap calls within a single mount.

**Same-key severity escalation is architecturally deterministic under daily-close replay — a real finding from building Phase 4's live demo, not a bug.** Each `(ticker, trading_day, signal_type)` is scored from a fixed close price and a fixed (static, Phase 2) baseline — re-scoring the same trading day at a later wall-clock time always reproduces the identical z_score and severity, because neither input has changed. This means genuine escalation for a fixed key **cannot** occur just by letting replay resume after a `stale`/`outage` fault and waiting — verified directly: recovering from a freeze and letting the scheduler naturally advance past the frozen tick reproduced the same severity every time. A real intraday feed (multiple ticks arriving through a single trading session, each with a different return-so-far) would exhibit genuine escalation organically; this project's one-close-per-day replay granularity does not have that texture. Documented here and in RELIABILITY.md rather than silently worked around. `backend/scripts/seed_demo_escalation_precondition.py` exists specifically to make the ack-bust mechanism observable live within a 5-minute demo window despite this: it seeds a single flag at an artificially low severity for a real, not-yet-reached (ticker, trading_day) and acks it — verifying beforehand, using the real unmodified scoring functions, what the true severity will be, and refusing to seed if the placeholder wouldn't actually be superseded. Everything downstream of that one constructed starting row (the correction, the ack-bust, the new severity) is 100% genuine, computed by the unmodified `run_scoring_cycle`/`upsert_flag_with_ack_bust` from real historical data, fired by the live scheduler — not a manual script call. Verified live on 2026-09-04 (see below).

**Demo-user identity is a single seam (`app/auth.py`), not real auth.** `get_current_user()` always returns one hardcoded UUID constant (`DEMO_USER_ID`). Every watchlist endpoint obtains the current user only through this dependency (never off a request field or invented inline), and checks `watchlist.user_id == current_user` via `app/services/watchlist_access.py::get_owned_watchlist()` — a non-existent and a non-owned watchlist both 404 identically, no resource-existence leak. When real JWT verification is built, only `get_current_user()`'s body changes (decode the token, look up the user) — no endpoint logic or data model change required, because every call site already goes through this one function.

**Aggregate ETag hash — canonical scheme, locked.** `app/services/digest.py::compute_aggregate_hash()`: for every currently unacknowledged flag belonging to the watchlist's tickers, take `(flag_id, severity_rank)`, sort by `flag_id` ascending, serialize as `json.dumps([[flag_id, severity_rank], ...], separators=(',', ':'))`, SHA-256 hex digest of the UTF-8 bytes. Always recomputed fresh from committed DB state (the same join shape as DATA_MODEL.md's "what's changed since last check" query) — never cached, never trusts a client-supplied hash. Empty set hashes to a stable, defined value (`EMPTY_DIGEST_HASH`), not an error.

**`GET /digest` 304 semantics implemented via FastAPI's `Request`/`Response` directly**, not a framework-level ETag middleware — `If-None-Match` is compared against the freshly-computed hash per request; both the 200 and 304 paths set the `ETag` header explicitly.

**Evidence's mean_return/stdev_return/points now come from the exact same baseline row that produced the flag's stored z_score — corrected same-day, before commit.** The original Phase 3 implementation recomputed a 30-return window ending at the flag's own `trading_day` independently of the baseline used at scoring time; because Phase 2's baseline is static, that window usually didn't match the one the stored z_score actually came from, so the displayed band and the displayed z_score could be mutually inconsistent — on the one screen whose entire purpose is proving the system isn't a black box. Fixed in `app/services/evidence.py`: `mean_return`/`stdev_return` are read directly from `load_latest_baseline()` (the same call the scoring engine used), and `points` is the exact window that baseline's own stats were computed from. The flagged day's own return is fetched separately (it may fall outside that plotted window, since the baseline is static) — `flagged_point.z_score` is the flag's stored value, and it is now algebraically reproducible as `(flagged_point.return - mean_return) / stdev_return` within floating-point/NUMERIC(6,3)-rounding tolerance. Verified both in a dedicated test (`test_flagged_point_zscore_is_reproducible_from_response`) and manually against a real Phase 2 flag (EICHERMOT.NS, 2025-09-05): recomputed z = 2.178039 vs. stored 2.178, diff ~4e-5.

**Live APScheduler background job, replacing the "manual trigger only" state from Phase 2/3.** `backend/app/main.py`'s lifespan constructs the provider once, starts an `AsyncIOScheduler` job (`SCHEDULER_INTERVAL_SECONDS`, default 5s) that calls `run_scoring_cycle` then `provider.advance()`, and shuts it down cleanly on app shutdown. `run_scoring_cycle` (`app/services/scoring_pipeline.py`) is the exact per-tick logic extracted from Phase 2's `run_scoring_once.py` — both the manual script and the live scheduler call this one function, never duplicated. Test suite disables the scheduler job (`settings.scheduler_enabled = False`, still constructs the provider) so tests aren't racing a real 5s-interval background job; they exercise `run_scoring_cycle` directly instead, which the scheduler itself just calls on a timer.

**Freeze semantics — precise, per Phase 4's locked decisions.** `FaultInjectingProvider` (`app/providers/fault_injecting.py`) wraps `HistoricalReplayProvider`: `'stale'` freezes the replay position (`advance()` no-ops) but does NOT touch `age_seconds`, which is always real wall-clock time since `last_successful_fetch` — `age_seconds` grows naturally and crosses the configured thresholds purely because real time passes while the replay position doesn't move (verified live: age reached 48.7s real elapsed time while frozen). `'outage'` is a harder failure — `get_ticks()` raises, `get_status()` reports `UNAVAILABLE` immediately regardless of age. `'recover'` transitions back to `'normal'` but does NOT snap freshness back to LIVE — it stays whatever real elapsed time dictates until the next successful `get_ticks()` call refreshes `last_successful_fetch` (confirmed live: status stayed briefly non-LIVE immediately after recovery, then flipped to LIVE once the next natural scheduler tick fired). The scheduler's own interval is never altered by fault injection — `advance()` is simply a no-op internally while frozen, so the scheduler doesn't need to branch on fault mode at all.

**Freshness thresholds are demo-compressed, one configurable mechanism (`FRESHNESS_LIVE_SECONDS`/`_RECENT_SECONDS`/`_DELAYED_SECONDS`/`_STALE_SECONDS`, `app/config.py`).** Defaults: 5s/15s/30s/30s. A production deployment against real intraday/EOD equity data would reasonably use the original minutes-scale figures (15s/2min/10min); these are shortened specifically so live fault-injection is demonstrable within a 5-minute presentation instead of a 10+ minute real wait. No hardcoded threshold values anywhere in the freshness-state code (`app/services/freshness.py`) — see ARCHITECTURE.md.

**`provider_state` persists `mode`, `frozen_at`, AND `last_successful_fetch` across a process restart — corrected same-day, before commit.** The original Phase 4 implementation persisted only `mode`/`frozen_at` and documented the `last_successful_fetch` gap as a narrow, low-risk limitation. That claim was checked empirically with a simulated restart (see below) rather than taken on faith, and it was WRONG: freshness classification depends entirely on `age = now - last_successful_fetch` — `frozen_at` is cosmetic, only used in `detail`, and never enters the state computation. So a restart during a genuine `'stale'` fault reset the age clock to the restart moment and `get_status()` immediately reported **LIVE** despite the feed having been stale for real minutes beforehand — a correctness bug, not a narrow limitation. `'outage'` was independently confirmed safe across restart (its `UNAVAILABLE` branch never consults `age_seconds`). Fixed by adding a `last_successful_fetch` column (`migrations/002_add_last_successful_fetch.sql`) and persisting all three fields together via `FaultInjectingProvider.persist()` — called on every `/admin/fault` change and on every scheduler tick (bounding an unclean crash's freshness-precision loss to one scheduler interval, not the whole restart). Re-verified after the fix with the same simulated-restart method: `STALE` with `age_seconds > 290` correctly survived the simulated restart; two dedicated regression tests added (`test_restart_during_stale_correctly_preserves_staleness_not_reset_to_live`, `test_restart_during_outage_remains_unavailable`). Concurrent `/admin/fault` calls remain last-write-wins (a single UPSERT on the fixed `id=1` row) — no locking, unchanged.

**Manual trigger script, still available, now calls the shared pipeline.** `backend/scripts/run_scoring_once.py` still works standalone (its own plain, unwrapped `HistoricalReplayProvider`) but now calls `run_scoring_cycle` instead of duplicating the scoring loop.

**Baselines were static across replay days through Phase 6 (Phase 2 simplification) — resolved by Workstream 1 (2026-09-05, see entry below).** Phase 1's backfill computed exactly one baseline row per ticker; nothing recomputed per replay day until Workstream 1. Note the Phase 4 consequence below (same-key severity escalation cannot occur from natural replay resumption alone) is about the replay's one-close-per-day granularity, NOT about static baselines — it still holds after Workstream 1, since a fixed trading day's rolling baseline is itself now correctly deterministic (computed once per day from the same prior data every time that day is re-scored), not because it's stale.

**`trading_day` derives from the replay tick's own timestamp, never wall-clock time (Phase 2 decision, still load-bearing).** `scoring.trading_day_from_tick()` returns `tick.timestamp.date()`. Confirmed again in the Phase 4 live run: flags carried dates like `2026-02-16`, not `2026-09-04`.

## Recent changes
- Sep 6: **Step B — Event Grouping, a pure read-time aggregation layer over the digest, no scoring/storage/ack-model changes.** Clusters currently-surfaced sector-wide signals by sector so a genuine multi-stock sector move reads as one event instead of N separate rows.
  - **`app/services/digest.py::compute_events()`**: a pure function over the rows `_UNACKED_FLAGS_SQL` already returns (post-Step-A: one unacked flag per ticker) — no new table, no persistence, recomputed fresh every `GET /digest` call. Only `sector_relative == 'sector_wide'` flags are eligible; stock-specific flags are never grouped, regardless of sector. **Blast-radius limit, explicit and tested:** a sector needs 2+ surfaced members to become an event — a lone sector-wide flag renders as an ordinary row, unchanged, keeping the common case visually identical to before this feature.
  - **API**: `GET /digest` gains `events: [{sector, tickers, strongest_z_score}]`, sorted strongest-cluster-first. `flags` is untouched — `events` is purely supplementary, never a replacement of the flat list.
  - **Frontend**: new `DigestEventCard.tsx` — collapsed by default, expands to reveal the real `DigestRow` for each member (reused, not reinvented, so ack/select/evidence work identically inside an event). `DigestList.tsx::buildDigestItems()` walks `flags` in existing rank order, substituting one event card at the position of the cluster's first (highest-ranked) member and skipping that cluster's remaining rows as standalones. **No bulk-ack-by-event** — acking a member inside an expanded card calls the same per-flag `onAck` as any other row.
  - **Testing**: 5 new backend unit tests (`test_events.py`: 2+-member clustering, single-member exclusion, stock-specific exclusion, multi-sector strongest-first ordering, mixed sector-wide/stock-specific in the same sector), 3 new backend API integration tests (`test_digest_api.py`: real DB-joined grouping via `test_ticker`/`test_ticker_2` sharing a sector, single-member/stock-specific exclusion at the API level, acking one member correctly drops the cluster back below 2 without affecting its sibling). 4 new frontend tests (`DigestList.test.tsx`: single-member renders as an ordinary row, a real cluster renders collapsed then expands to real rows, acking a member row calls `onAck` with only that flag's id, a stock-specific flag never gets swept into a same-sector event). Full suite: backend 155 → 163, frontend 57 → 61, `tsc --noEmit` and `npm run build` both clean.
  - **Live-verified against the real running system**: added a second Banking/Finance ticker (ICICIBANK.NS) to the real demo watchlist and boosted two real flags' severity so each ticker's single surfaced digest slot was the sector-wide one — the live `GET /digest` response then correctly returned `events: [{"sector": "Banking/Finance", "tickers": ["HDFCBANK.NS", "ICICIBANK.NS"], "strongest_z_score": 6.5}]`. Confirmed `events: []` both before this setup and after reverting it (test ticker/flags removed from the watchlist and DB), and confirmed a single-member sector never appeared in `events` throughout.
- Sep 6: **Step A — "since you last checked," made literal.** Additive capability surfacing how much a resurfaced (escalated-then-unacked) flag's severity/z_score changed since it was last acked, using data the ack-bust mechanism already produced but previously discarded.
  - **Schema** (`backend/migrations/002_since_last_checked.sql`): `flag_ack` gains nullable `severity_rank_at_ack`/`z_score_at_ack`, populated at ack time from the flag's then-current values (`POST /ack`, `app/routers/watchlists.py`). New audit-only table `flag_ack_history` (`flag_id`, `watchlist_id`, the same two snapshot columns, `acked_at`, `superseded_at`) — never read by ack-bust logic, never affects it.
  - **`app/services/flags.py`**: exactly one statement added to the existing ack-bust transaction, immediately before the existing `DELETE FROM flag_ack` — copies that row's current values into `flag_ack_history` with `superseded_at = now()`. The delete's condition, timing, and transaction boundary are unchanged (verified: all pre-existing ack-bust tests pass unmodified).
  - **`app/services/digest.py::load_since_last_ack()`**: per flag_id, the live `flag_ack` row if currently acked, else the most recent `flag_ack_history` row if previously acked and since busted — a flag with neither yields no entry, never a fabricated baseline. `GET /digest`'s `since_last_ack` field is `null` unless a snapshot exists AND differs from the flag's current values.
  - **Frontend**: `Flag.since_last_ack` type, `formatSinceLastAck()` (`lib/severity.ts`), a small delta line under the z-score hero number in `DigestRow.tsx` (e.g. "↑ 1.7σ since you last checked · was SIGNIFICANT, now EXTREME"), rendered only when present — no new card type, no layout change.
  - **Two real bugs caught before commit**: (1) the new `flag_ack_history` FK broke test-fixture teardown ordering (`conftest.py` deleted `flags`/`watchlists` before clearing the audit table referencing them) — fixed by adding scoped `DELETE FROM flag_ack_history WHERE flag_id/watchlist_id = ...` before each existing cleanup step, mirroring the pre-existing `flag_ack` cleanup pattern exactly (verified the fix is correctly scoped, not a blanket `DELETE FROM flag_ack_history`, before treating it as safe). (2) The `since_last_ack` lookup's `UNION ALL` query had an `ORDER BY ... superseded_at DESC` that Postgres bound to the whole union rather than just the `DISTINCT ON` subquery it was meant for (`superseded_at` isn't a column of the union's output) — fixed by wrapping that branch in its own subquery.
  - **Known gap, deliberately not fixed this session**: `flag_ack_history.flag_id`/`watchlist_id` have no `ON DELETE CASCADE`, and no code path in the real app currently deletes a `flags` or `watchlists` row (only `DELETE /watchlists/{id}/items/{ticker}`, which doesn't touch this table) — so no orphan risk exists today. But a future watchlist-delete or flag-purge feature would need to explicitly clean up `flag_ack_history` first (same pattern the test fixtures now follow) or it would leave real orphaned audit rows, undermining the table's whole purpose as a trustworthy audit trail. Flagged in DATA_MODEL.md rather than fixed speculatively.
  - **Testing**: 5 new backend tests (`test_flags_ack_bust.py`: history-copy-before-delete with correct snapshot values, de-escalation creates no history entry and leaves the live ack untouched; `test_digest_api.py`: resurfaced flag's digest response carries the correct pre-escalation `since_last_ack`, a flag with no ack history returns `null`). Full suite 155/155 passing (was 152), frontend 57/57, `tsc --noEmit` clean.
  - **Live-verified against the real running system**: acked a real flag (HCLTECH.NS, rank 2/z=3.447), escalated it via the actual `upsert_flag_with_ack_bust` (not a script) to rank 3/z=-5.1 — confirmed `ack_busted=True` and the live digest response showed `since_last_ack: {severity_rank_at_ack: 2, z_score_at_ack: 3.447, ...}` against the new current values. Reverted the test data afterward; real seeded/demo state confirmed otherwise untouched.
- Sep 5: **UI Reference Adoption — ui_reference.md's visual/structural design ported onto the real frontend, 11 sequential commits, zero backend files touched, zero real data flows regressed.** `ui_reference.md` (a Lovable-generated TanStack Router prototype, fully mocked data) was treated strictly as a visual/structural reference — every real hook, endpoint, and behavior was preserved; its data layer and routing were never copied.
  - **Design tokens** (Step 1): OKLCH palette + IBM Plex Sans/Mono + Plus Jakarta Sans fonts, ported onto the existing token *names* (only values changed, so every component kept working through every later step). Single committed light theme preserved — the reference's `.dark` variant deliberately not ported.
  - **SeverityBadge/DirectionArrow** (Step 2): severity now carries its own hue (new notable/significant/extreme tokens, lucide icons) instead of the old direction-hue/severity-intensity two-axis model; direction split into its own icon component.
  - **FreshnessIndicator** (Step 3): FreshnessBanner rewritten as a colored status dot (pulsing when LIVE) + eyebrow label/detail line, all 5 real states wired to `useProviderStatus`. The old generic `Badge` component had no other callers, so it was deleted rather than left dead.
  - **SignalRow/SignalList** (Step 4): |z|σ + direction leads each row, bordered/shadow container, richer empty state. Reference fields with no real backend equivalent (price, change%, company name, sector on the row) were dropped rather than fabricated.
  - **NewSignalsBanner** (Step 5): the "N new signals" pull-in affordance extracted into its own component — same real `viewed`-vs-`latest` digest-poll mechanism, only the presentation moved.
  - **WatchlistPanel** (Step 6): the `<select>` dropdown replaced with live search-as-you-type over the real `GET /tickers` universe; icon-button row actions. History still opens the existing real History dialog.
  - **HistoryList** (Step 7): row structure restyled (date + severity/signal-type/direction + sigma badge); the reference's "stock-specific/sector-wide" subtitle segment dropped since `GET /tickers/{ticker}/flags` rows don't carry that field.
  - **EvidenceDrawer/DistributionChart** (Step 8) — two decisions reported before proceeding, per instructions: (1) the real `/evidence` endpoint returns raw points, not the reference's pre-bucketed histogram — bucketed client-side (`lib/histogram.ts`, unit-tested), no backend change needed. (2) The reference's chart uses recharts; ARCHITECTURE.md documents a deliberate "hand-rolled SVG, no charting library" decision for this exact chart, so the reference's visual structure (histogram + ±2σ band + "Today" reference line) was ported as hand-rolled SVG instead, no new dependency added. Now a right-side drawer with a real stat grid, real sector-relative text, an inline "earlier signals" list, and its own working Acknowledge action (see "Next steps" #5 above — this incidentally fixed that known gap).
  - **TodaysBrief** (Step 9): larger eyebrow+headline treatment + a real stock-specific/sector-wide stats row (computed from the current flags) — the headline sentence itself is still rendered verbatim from the real deterministic `brief` field, never regenerated client-side (the reference's own sentence-generation logic was discarded, per PRODUCT.md's locked decision). The reference's "acknowledged" stat was dropped (no real number backs it, since the digest only returns unacked flags); the "60-day" threshold caption text was corrected to the real 30-day rolling window.
  - **AboutDialog** (Step 10): existing real copy (including the two Groww engineering-post citations) restructured into eyebrow-labeled topic sections, unchanged word-for-word; added one new factual section (real severity band thresholds from `SEVERITY_BANDS`).
  - **AppHeader/DemoControls** (Step 11, wires everything together): sticky header with icon badge + right-aligned About/Demo/freshness cluster, around the existing two-tone wordmark (kept exactly as delivered in Workstream 4). Fault injection moved from an always-visible panel into a discreet header popover, still calling the real `/admin/fault` endpoint, still gated on the real `demo_mode` field (this gating doesn't exist in the reference — added, not lost). The reference's multi-watchlist switcher and header "Add stock" trigger were both deliberately not ported (no multi-watchlist workstream exists; WatchlistManager already has real inline search) — explained inline rather than shipped as dead UI.
  - **Full regression pass (Step 12)**: backend 150/150 passing (unchanged — zero backend files touched across all 11 commits), frontend 55/55 passing (up from 46 at the start, net new coverage for every restyled component), build clean. Live in-browser: fresh load → real severity hierarchy (Extreme > Significant > Notable, visually distinct by color/stripe-width/badge) → evidence drawer → acknowledge (both from the row and from the drawer) → Today's Brief updating in real time → fault injection (outage) → recovery, all confirmed against a real running backend/Postgres, cleaned up afterward via `reset_demo_state.py`.
- Sep 5: **Automated E2E demo rehearsal (Playwright) — demo-readiness is now verifiable by running one command.** `frontend/e2e/demo-rehearsal.spec.ts` walks the canonical demo script (see the new section at the top of this file) end to end against a real running backend, real Postgres, and the real frontend — no mocks anywhere.
  - **Setup added**: `@playwright/test` (frontend devDependency), `frontend/playwright.config.ts` (`webServer` entries for backend `uvicorn` and frontend `next dev`, both `reuseExistingServer: true` so it attaches to servers you already have running rather than double-starting — relevant given this session found leftover dev servers from earlier work more than once), `npm run test:e2e` script.
  - **New backend helper, read-only**: `backend/scripts/find_escalation_candidate.py` — dynamically finds the nearest real (ticker, trading_day) beyond a given replay step whose engine-computed severity crosses a threshold, using the exact same look-ahead-safe functions the live pipeline uses. Needed because the escalation-flip beat requires a real not-yet-reached day, and which day qualifies depends on wherever the live server's replay clock currently sits — unknowable ahead of time, especially for a reused long-running server, so it can't be a hardcoded fixture.
  - **Real bug found and fixed while building this**: neither this new script nor the existing `seed_demo_escalation_precondition.py` checked `sample_size >= MIN_SAMPLE_SIZE` before "verifying" a day's real severity — both computed `z_score`/`severity_band` directly without the same confidence gate `score_price_zscore()` (the real pipeline) enforces. A day with insufficient history could have been "verified" and seeded as an escalation target that the real pipeline would actually suppress entirely — the placeholder would never get corrected, and a live demo would hang forever waiting for a flip that can't happen. Fixed in both scripts (identical gate check, matching the real pipeline exactly); 2 new regression tests (`test_escalation_scripts_sample_size_gate.py`). Full backend suite: 148 → 150 passing.
  - **Test design decisions worth recording**: (1) the digest ambiguously has TWO `<li>` elements that can match a ticker's bare name — the watchlist-manager list and the digest list — disambiguated by filtering for the presence of an "Acknowledge" button (only digest rows have one). (2) Real flags are fast-forwarded via `scripts.run_scoring_once` (the exact function the scheduler calls, just invoked synchronously with a throwaway provider instance) rather than waiting on live wall-clock scheduler ticks for the first assertions — this doesn't touch the live server's own in-memory replay clock, which is checked independently via `/provider/status` before picking the escalation target. (3) The digest deliberately buffers background updates behind a "Show N new signals" affordance (Stage 2 UX) — waiting for the escalated flag required a poll-and-click loop (`waitByPullingInNewSignals`), not a plain `toBeVisible` wait, since a background poll revealing it doesn't auto-render it. (4) The Today's Brief assertion originally checked for the "extreme-severity move" clause unconditionally — a real, single-run failure showed this is wrong whenever exactly one signal ends up active (the single-signal template never mentions severity by design, a valid outcome), so the assertion now checks that the brief names the escalated ticker specifically, which holds across all three of its template branches.
  - **Verified real, three times** (not just designed): two full `npx playwright test` runs plus one via the documented `npm run test:e2e` command, all passed, ~34s each, no flakiness observed across the runs. The one genuinely timing-sensitive step (waiting for the live scheduler to reach the escalation target) is bounded at 150s, not indefinite.
  - Docs updated: PROGRESS.md (this entry, the new "Canonical demo script" section, checklist), README.md (§11 new "E2E demo rehearsal" subsection + test counts).
- Sep 5: **"Today's Brief" — deterministic template synthesis over the current digest, no LLM.** A short (1–3 sentence) summary of the current unacknowledged signals, computed purely from the same rows `GET /digest` already fetches — no network call, no external dependency, nothing that can time out because there's nothing external to fail.
  - **API design decision (reported as required): extended `GET /digest`'s existing response with a `brief` field, rather than adding a new endpoint.** `GET /digest` already fetches exactly the rows the brief needs in one query; a separate `GET /brief` would either duplicate that query or require passing digest data back in as a request body, for zero benefit (unlike an LLM call, there's no cost/latency reason to decouple). One added field, one join (`flags` → `tickers`, for the sector name used in the concentration fact), no new route, no new frontend polling hook.
  - **Implementation** (`backend/app/services/brief.py`): `compute_brief_facts()` computes the six required deterministic facts (total count, counts by severity, counts by sector + which sector reaches "concentration" — ≥2 signals, the single strongest signal by |z_score|, direction split up/down, whether the strongest signal is sector-wide/stock-specific) purely from the flag rows passed in — order-independent and tie-broken deterministically (ticker alphabetical) so identical input always produces identical output regardless of caller ordering. `render_brief()` is pure template synthesis over those facts: a dedicated single-signal sentence, a "concentrated in {sector}" branch when a sector reaches ≥2, a "spread across sectors" branch otherwise, and an appended "including N extreme-severity move(s)" clause only when at least one flag is actually extreme (never padded in otherwise). Zero active signals → `None`, not an empty/awkward string — the frontend's existing calm empty-state copy is unchanged.
  - **Wiring**: `app/routers/watchlists.py`'s `GET /{watchlist_id}/digest` calls `build_brief(rows)` on the exact rows already used for `_serialize_flag()` and the ETag hash — the brief cannot influence the hash (unaffected, still only `(flag_id, severity_rank)` pairs) or vice versa. `frontend/components/DigestBrief.tsx` renders it as a quiet line (a `label-caps` "Today" micro-label + one sentence, `text-sm text-ink`) directly under the header/tagline — no icon, no "AI" framing, no gradient/chat-bubble treatment; renders nothing (not even a placeholder) when `brief` is `null`.
  - **Two real bugs caught by the brief's own test suite before they shipped**: (1) the tie-break for "strongest signal" used `max()` over `(|z|, ticker)`, which actually picks the alphabetically-*last* ticker on a tie, not the first — fixed to `min()` over `(-|z|, ticker)`. (2) A test asserting "no mention of 'extreme'" false-failed against the spread-branch's own base wording ("...is the most **extreme**, at Xσ") — not a code bug, but revealed the test needed to check for the padded clause specifically (`"extreme-severity"`) rather than the bare word, since "extreme" legitimately appears in that branch's normal template text too.
  - **Testing**: 23 new tests — 19 unit (`test_brief.py`: all six facts' correctness, every template branch, extreme-clause presence/absence/pluralization, no causal/trading language, determinism re-run, order-independence) + 4 integration (`test_brief_api.py`, real Postgres: null at zero signals, correct single-signal text, and the full ack-down-to-one-then-zero sequence confirming the brief tracks real committed state, not a snapshot). Frontend: 3 new (`DigestBrief.test.tsx`: renders text+label, renders nothing at null, no AI/robot/sparkle framing anywhere in its output). Full suite: backend 123 → 146, frontend 43 → 46, all passing, zero regressions.
  - **Live verification** (real backend + frontend, demo_mode on): a real 2-signal digest (both POWERGRID/ASIANPAINT-style concentrated-sector cases) rendered the concentrated-sector template correctly; acked down to one signal → single-signal template fired correctly; acked the last one → brief disappeared entirely, only the existing "Nothing unusual right now" copy remained, no broken/empty line. Ran the fault-injection escalation-flip sequence (`seed_demo_escalation_precondition.py` against POWERGRID.NS/2026-02-02, a real day independently verified to reach `extreme`/z=4.651) — after the live scheduler naturally reached that day, the brief correctly updated to "Ten unusual moves today, concentrated in Energy/Materials — POWERGRID is the most statistically unusual, including one extreme-severity move," reflecting the resurfaced escalated flag. Cleaned up afterward (`reset_demo_state.py`; the ticker temporarily added to the demo watchlist for this test was removed again; real seeded data confirmed untouched).
  - **Follow-up fix, same day: sector concentration now counts distinct tickers, not flag rows.** The judgment call flagged above ("Two unusual moves... concentrated in Consumer/FMCG" from two ASIANPAINT-only flags) was corrected: `compute_brief_facts()` now dedupes by ticker per sector (`sector_tickers: dict[str, set[str]]`) before counting toward "concentration." Also fixed a related wording bug the fix surfaced: the non-concentrated fallback branch claimed "across different sectors" unconditionally, which was false for the same-ticker-repeated case (only one sector actually involved) — now branches on whether ≥2 *distinct* sectors are genuinely present, using a neutral "N signals today; ..." wording otherwise. 2 new tests (`test_facts_two_flags_for_the_same_ticker_do_not_count_as_concentration`, `test_render_two_flags_same_ticker_same_sector_uses_neutral_branch_not_concentrated`); full suite 146 → 148 passing.
  - **`volatility_regime` decision, made explicitly rather than left deferred:** stays out of the digest — but PRODUCT.md/README's rationale was stale (still citing "repetitive noise," which Workstream 1 already resolved) and has been rewritten to the real, current reason: `GET /digest`'s `ORDER BY abs(z_score) DESC` and Today's Brief's z-score-based "strongest signal"/direction facts both assume every digest row has a real z-score, which `volatility_regime` doesn't. Re-enabling it is still a real future improvement (README §13), but now correctly scoped as "decide how these two z-score-dependent consumers should treat a non-directional signal," not "flip a one-line filter."
  - Docs updated: PROGRESS.md (this entry + checklist), PRODUCT.md (new "Today's Brief" section, explicitly distinguished from the still-unbuilt optional LLM-phrasing layer), README.md (§5 feature bullet, §6 API-design rationale, §11 test counts + a highlighted test).
- Sep 5: **Workstream 3 — optional, opt-in `LiveDelayedNSEProvider`.** Additive-only second `MarketDataProvider` implementation, proving the abstraction generalizes to a genuinely unreliable real source without special-casing — `HistoricalReplayProvider` remains the default and the only provider the demo depends on.
  - **Real verification performed before building anything** (per the workstream's own instruction not to assume the first idea works): a plain request to `https://www.nseindia.com/` from this environment returned an immediate `403` (bot/Cloudflare protection) — a dead end, not pursued further. `yfinance`'s intraday endpoint (`Ticker.history(period="1d", interval="1m")`) was tried instead and **actually works**: a real, unmocked call against `RELIANCE.NS` returned 361 real 1-minute bars for the most recent trading session. This became the provider's data source.
  - **Implementation** (`backend/app/providers/live_delayed_nse.py`): `LiveDelayedNSEProvider` implements the exact `MarketDataProvider` protocol (`get_ticks`, `get_status`), tags emitted ticks `source="live_delayed_unofficial"` (added to `Tick`'s Literal in `app/providers/base.py`, ARCHITECTURE.md updated). No new error-handling path: `get_status()` reuses `classify_freshness()` — the identical function `FaultInjectingProvider` already uses — reporting `UNAVAILABLE` before any fetch has ever succeeded (mirroring `FaultInjectingProvider`'s own hard-failure branch, not a new state) and `STALE` once `age_seconds` grows past the same configured thresholds. A single ticker's fetch failure (exception, empty response, malformed row) is swallowed and simply excluded from that batch — the same shape as `HistoricalReplayProvider` skipping an exhausted ticker — never aborting the whole call. One request per ticker, a 5s timeout, no retry/backoff.
  - **Design question raised explicitly, not assumed (locked decision #5): should live-delayed ticks be scored?** Recommended and implemented **no** — status-only. Ticks are reachable only via the new `GET /provider/live-status` (gated behind `LIVE_PROVIDER_ENABLED`, 404s unconditionally when unset, same convention as `/admin/fault`'s `DEMO_MODE` gate); they never reach `price_ticks`, baseline computation, or the scoring pipeline. Reasoning: mixing a fragile, occasionally-empty real feed into the same rolling baseline the whole scoring engine now depends on (Workstream 1) is a real correctness risk for zero demo benefit — the abstraction point is already fully proven by a real `get_ticks()`/`get_status()` call succeeding or failing honestly through the existing freshness states.
  - **Wiring** (`app/main.py`): `app.state.live_provider` is constructed only when `settings.live_provider_enabled` is true, else explicitly `None` (so accidental access fails loudly). Entirely separate from `app.state.provider` (the replay/fault-injection provider the scheduler and scoring pipeline use) — never substituted, never touched by `run_scoring_cycle` or the scheduler job.
  - **Testing:** 8 new tests (`test_live_delayed_provider.py`) — unit-level (`UNAVAILABLE` before any successful fetch, a swallowed per-ticker exception, a real successful-fetch shape check, one ticker's failure not aborting the batch, an empty-DataFrame response treated as "no usable data" not an exception) and integration-level against the real FastAPI app (`/provider/live-status` 404s and `app.state.live_provider` is `None` with the flag off — the default — and, with the flag toggled on for one test only, the app starts correctly and a real fetch attempt provably occurs through the full route → provider → `get_ticks()`/`get_status()` path, yfinance itself mocked for CI determinism). Full suite: 123/123 passing (was 115), zero regressions with the flag off (its default).
  - **Real end-to-end verification, unmocked**, run manually against a live server process with `LIVE_PROVIDER_ENABLED=true`: `GET /provider/live-status?ticker=RELIANCE.NS` returned real data — `{"state":"LIVE","tick":{"ticker":"RELIANCE.NS","price":1322.0,"volume":635236,...,"source":"live_delayed_unofficial"}}`. A three-ticker real call (`RELIANCE.NS`, `INFY.NS`, a deliberately-invalid symbol) returned 2/3 successfully and reported the third's failure in `detail` without raising or special-casing. A fresh process with the flag unset (default) 404s the route immediately, confirmed via direct HTTP request.
  - Docs updated: PROGRESS.md (this entry + checklist), PRODUCT.md (new "Optional, opt-in second provider" subsection under "Data source — locked"), README.md (§4 architecture note + a future-improvements item), ARCHITECTURE.md (Tick's source Literal, provider list's #3, API table row, folder structure entry).
- Sep 5: **Workstream 4 — in-product brand identity + historical changes log.** Two independent, small product-completeness additions; the existing Digest experience (scoring, evidence, ack, freshness, polling, fault injection, scheduler, digest API) was not touched.
  - **Part A (brand identity):** `frontend/components/AppHeader.tsx` — a proper wordmark treatment ("signal" in muted ink, "Digest" in full ink weight, design.md's headline-md/headline-lg type steps at mobile/desktop) plus PRODUCT.md's own core-promise tagline verbatim ("Know in five seconds what actually changed — not what always jitters — and why."), replacing the plain `<h1>Signal Digest</h1>` in `page.tsx`. `frontend/components/AboutPanel.tsx` — a small accessible dialog (built on a new shared `frontend/components/Dialog.tsx`: `role="dialog"`/`aria-modal`, Escape closes, backdrop click closes, a click inside the panel never reaches the backdrop, focus moves into the panel on open and returns to the trigger on close) with content drawn directly from PRODUCT.md: what the product does, why it differs from a conventional watchlist, why scoring is deterministic, why "since you last checked" is central (citing both Groww engineering posts already cited in PRODUCT.md), and an explicit closing line disclaiming live-market-data/investment-advice claims.
  - **Part B (historical changes log):** New endpoint `GET /tickers/{ticker}/flags` (`backend/app/routers/tickers.py`) — read-only, unauthenticated (universe-level, not user data, same convention as `GET /tickers`), returns every recorded flag for a ticker (`trading_day`, `signal_type`, `z_score`, `severity` only) in reverse chronological order (`ORDER BY trading_day DESC, computed_at DESC`); 404s for an unknown ticker. Explicitly independent of the digest/ack state — a fully-acked or never-watchlisted ticker still shows its full history. `frontend/components/HistoryPanel.tsx` renders it using the `Dialog` shell, design.md's edge-to-edge/hairline-divider/tabular-numeral list-row treatment (no charts, cards, or summary stats), and an intentional empty state ("Nothing unusual has been recorded for this ticker.") distinct from the error state. Reached via a new "View history" link per ticker row in `WatchlistManager.tsx` (chosen over the Digest row because a ticker can have real history while having zero current active flags — the digest only lists currently-flagged tickers, so it can't be the sole entry point).
  - **Testing:** backend 109 → 115 passing (6 new tests for the endpoint: reverse-chronological ordering, exact field set, empty-ticker case, unknown-ticker 404, cross-ticker isolation, and — the one that actually proves the "history ≠ digest state" requirement — a fully-acked flag still appears in history). Frontend 29 → 43 passing (14 new: wordmark/tagline rendering, About-click wiring, AboutPanel accessible-dialog/backdrop/Escape/content-sourcing behavior, HistoryPanel real-data rendering/empty-state/error-state/accessible-dialog behavior, and WatchlistManager's new "View history" wiring). `npx tsc --noEmit` and `npm run build` both clean.
  - **Browser-verified live** (real backend + frontend dev servers, not just component tests): wordmark/tagline render correctly above the untouched Freshness/Demo-controls/Watchlist/Digest layout; About panel opens/closes via both the close button and Escape with real backdrop-click-to-close behavior; "View history" on RELIANCE (added real flags via a short live scheduler run first) rendered a real 4-row reverse-chronological history — Oct 21 volatility_regime/notable down to Oct 08 price_zscore/notable with correct severity labels, z-score signs, and up/down coloring — then reset via `reset_demo_state.py` afterward (real seeded data confirmed untouched throughout).
  - Docs updated: PROGRESS.md (this entry + checklist), PRODUCT.md (new "In-product brand layer" note), README.md (§4 architecture diagram, listing the new frontend components).
- Sep 5: **Workstream 1 — rolling baseline recomputation, removing the project's most-cited known limitation.** `backend/app/data/baselines.py`: added `compute_baseline_as_of(ticker, points, as_of_date)` (look-ahead-safe — filters to points strictly before `as_of_date` before delegating to the existing `compute_baseline_from_series`) and `load_baseline_as_of(pool, ticker, as_of_date)` (exact per-day row lookup); `load_latest_baseline` kept, docstring updated to clarify it's for "whatever's most recent" use cases only (Phase 1 seed script), never for scoring/evidence. `backend/app/services/scoring_pipeline.py`: each cycle now recomputes and upserts a fresh look-ahead-safe baseline per ticker (`load_price_series` + `compute_baseline_as_of` + `upsert_baseline`) before scoring that ticker's tick, instead of reading a static `load_latest_baseline` row — inserts a new `(ticker, as_of_date)` row per trading day, never overwrites an earlier day's row. `backend/app/services/evidence.py`: switched to `load_baseline_as_of(ticker, flag.trading_day)` and made `_baseline_window_points` look-ahead-safe too, so the evidence panel's plotted band/points always match the exact baseline that scored that specific flag — no longer merely "whatever's most recently computed" (a real behavior change once baselines are per-day, not a hypothetical). `backend/scripts/seed_demo_escalation_precondition.py`: pre-verification now calls the same `compute_baseline_as_of` the live pipeline uses (previously called `load_latest_baseline`, which would have silently diverged from what the pipeline actually scores once baselines are per-day).
  - **Verification, not assumption:** full backend suite 103 → 109 passing (6 new: 3 pure unit tests in `test_baselines.py` proving look-ahead-safety and sample_size growth/capping, 3 real-Postgres integration tests in the new `test_rolling_baselines_integration.py` proving per-day row insertion, flag upsert correctness against a changing baseline, and a direct spot-check that a future violent price move doesn't leak into an earlier day's stored baseline stats). Two existing tests required adaptation, not because their intent changed but because the mechanism they exercised did: `test_reliability_partial_ingestion.py`'s mid-batch-crash simulation now monkeypatches `scoring_pipeline.load_price_series` (the function the pipeline now actually calls per-ticker) instead of the no-longer-called `load_latest_baseline`; two evidence tests in `test_digest_api.py` now seed their baseline via `compute_baseline_as_of`/`upsert_baseline` for the flag's own `trading_day` instead of `compute_and_store_baseline`'s old "as of the last date" convention, which would otherwise silently return `None` from `load_baseline_as_of` for any flag not on the series' final day.
  - **Ran the replay forward for real** (`python -m scripts.run_scoring_once --ticks 20 --start-step 25` against real seeded data, then `reset_demo_state.py` to clean up the resulting flags — baselines rows were intentionally left, since new rows are legitimate history per the locked "insert per day, never delete" decision): confirmed via direct SQL that `baselines` gained one new row per ticker per cycle (e.g. RELIANCE.NS: 21 rows spanning 2025-10-10 → 2026-09-04, `sample_size` climbing 24 → 30 across the earliest rows then holding at the 30-day cap), and spot-checked one day's stored `mean_return_30d`/`stdev_return_30d` against the surrounding real price series to confirm no look-ahead leakage.
  - **`volatility_regime` finding, reported as required (not just yes/no):** the flagged-ticker set now genuinely rotates across trading days instead of repeating the same handful every cycle — observed clusters shifted from `RELIANCE.NS`/`ICICIBANK.NS`/`WIPRO.NS`/`NESTLEIND.NS` (2025-10-21 to -10-28) to `NTPC.NS`/`POWERGRID.NS` (2025-10-31 to -11-06) to `MARUTI.NS`/`ADANIENT.NS`/`ASIANPAINT.NS`/`EICHERMOT.NS` (2025-11-07 to -11-10) over a single 20-cycle run. The original technical justification for excluding it from the digest (PRODUCT.md) no longer holds, but per the explicit scope of this workstream, `GET /digest`'s `price_zscore`-only filter was left untouched — re-enabling `volatility_regime` is a separate product decision for a future session.
  - Docs updated to match: PROGRESS.md (this entry + checklist + superseded architecture notes above), DATA_MODEL.md (`baselines` table description), PRODUCT.md (`volatility_regime` section), README.md (§5 volatility_regime finding, §6 baselines description, §8 trade-off reworded from "static" to "rolling," §9 scalability's forward-looking item, §11 test counts 103→109 backend / 132→138 total plus one new highlighted test, §12 limitations, §13 future improvements).
- Sep 4: **Full live demo rehearsal + two review fixes + Phase 6 reliability coverage.** (1) Ran the complete PRODUCT.md demo sequence live in one continuous browser flow, including the escalation-flip precondition script — all beats passed; one UX finding (ack-while-evidence-open isn't currently possible, not fixed, not required by the demo). Confirmed watchlist management is genuinely out of the demo's required scope, straight from PRODUCT.md's text. (2) Fix 1: `GET /digest` now filters to `signal_type='price_zscore'` only — `volatility_regime` still computed/persisted, just not surfaced (documented as a deliberate decision in PRODUCT.md, not a bug). Fix 2: added an explicit `ORDER BY abs(z_score) DESC` to the digest query (previously relied on incidental DB order — no test had caught this until now). 2 new tests, verified live via curl against a running backend (5 flags returned, all `price_zscore`, correctly descending). (3) Phase 6: closed test coverage on RELIABILITY.md scenarios #9/#10/#11/#12 against real Postgres. #10 (timezone) revealed and fixed a real bug — `trading_day_from_tick` took `.date()` on a UTC timestamp with no IST conversion, masked until now because real seeded data's 15:30 IST anchor never crossed the UTC/IST day boundary; fixed with `.astimezone(IST).date()`. #9 (partial ingestion), #11 (sector gating), #12 (stale ETag) all confirmed already correct by construction — no fixes needed. 6 new reliability tests. Full suite: 98/98 passing (was 90 before this session).
- Sep 4: **Phase 5 (frontend: digest, evidence, freshness, fault-injection) implemented and verified live via real browser automation, not curl.** STEP 0: `backend/scripts/reset_demo_state.py` created and run (cleared 2,093 accumulated flags to 0, real seeded data untouched), repopulated 122 real flags via `run_scoring_once.py` for frontend dev. STEP 1: two new backend endpoints — `GET /watchlists` (ownership-scoped) and `GET /tickers` (full universe, no auth), plus `demo_mode` added to `/provider/status` — 4 new backend tests, 90/90 passing. STEP 2-3: full frontend built — `frontend/lib/` (api client, types, severity/freshness badge config, deterministic explanation template, digest-poll reducer, dwell timer, bootstrap logic), `frontend/components/` (Badge, SeverityBadge, FreshnessBanner, FaultControl, DigestRow, DigestList, SkeletonRows, EvidencePanel with hand-rolled SVG chart), `frontend/hooks/` (useProviderStatus at 3s, useDigest at 5s with ETag), `frontend/app/page.tsx` orchestrating bootstrap + polling + ack-dwell + "new updates" affordance. STEP 4: vitest + Testing Library installed, 17 tests (severity/freshness config accessibility invariants, SeverityBadge component rendering, digest-poll 304-vs-200 reducer, dwell timer fake-timer tests, bootstrap idempotency with mocked API) — all passing, plus a clean `tsc --noEmit`. STEP 5: verified live with both servers running and real browser automation (claude-in-chrome): bootstrap fired exactly once (confirmed via DB watchlist count staying at 1 across a reload), evidence panel rendered a real flag's band+points+flagged-point correctly, ack via UI click wrote `flag_ack` and removed the row, and the fault sequence LIVE→Delayed(21s)→Stale(45s)→Unavailable→(recover)→LIVE was driven entirely through real button clicks and real polling, screenshotted at each stage. Full backend suite re-run clean at 90/90.
  - **Deferred (SHOULD/NICE, per priority instructions)**: watchlist add/remove typeahead, a dedicated empty/error state pass beyond what's already built (digest skeleton loading and evidence panel loading/error states ARE done), and the historical-changes-log screen. None of the MUST screens were compromised to attempt these.
- Sep 4: **Phase 4 correction (pre-commit review) — `last_successful_fetch` persistence bug found and fixed.** Empirically verified (simulated restart, not inferred from reading code) that a restart during a genuine `'stale'` fault incorrectly reported `LIVE` immediately after, because freshness depends only on `age = now - last_successful_fetch` and that field wasn't persisted. Added `migrations/002_add_last_successful_fetch.sql`, updated `app/services/provider_state.py` and `app/providers/fault_injecting.py` (new `persist()` method), wired into `/admin/fault` and every scheduler tick. Re-verified the fix empirically: `STALE` with `age_seconds > 290` now correctly survives a simulated restart. 2 new regression tests, 86/86 total passing. Also confirmed: `outage` was always safe across restart (unaffected by this bug). See the architecture note above for full detail.
  - **Flag accumulation note**: three uvicorn server processes from earlier verification sessions (this Phase 4 session and stray ones from Phase 0/3 work) were found still running in the background with their schedulers live, undetected until this review — killed during this check. Real `flags` count is currently **2,093** (609 `price_zscore` + 1,484 `volatility_regime`), well beyond the ~81 from Phase 2's original run. This is genuine scored output (not fabricated, not deleted per instruction), but Phase 5/demo prep will need a clean-slate strategy (e.g. `TRUNCATE flags, flag_ack, watchlist_ack_state` before a rehearsal or live run) — not yet implemented, flagging here as a to-do rather than building it now.
- Sep 4: **Phase 4 (fault injection + minimal live scheduler) implemented and verified live.** New: `app/services/freshness.py`, `app/providers/fault_injecting.py`, `app/services/provider_state.py`, `app/services/scoring_pipeline.py`, `app/routers/admin.py` (`POST /admin/fault`), `app/routers/provider_status.py` (`GET /provider/status`), `backend/scripts/seed_demo_escalation_precondition.py`. Modified: `app/main.py` (APScheduler lifecycle), `app/config.py` (freshness/scheduler env vars), `app/routers/watchlists.py` (real `freshness` field), `app/providers/historical_replay.py` (+`get_previous_price`), `run_scoring_once.py` (now calls the shared pipeline). 23 new tests (5 freshness unit, 10 FaultInjectingProvider unit/integration, 6 admin/provider API integration, 2 scheduler-freshness-gating integration), 84/84 total passing against real Postgres.
  - **Live demo sequence run end-to-end over real HTTP with the scheduler actually running** (uvicorn on :8003, `SCHEDULER_INTERVAL_SECONDS=5`, demo-compressed thresholds): initial status `LIVE` (age 2.48s, replay step 2) → acked a real `notable` flag (EICHERMOT.NS, id 14) → `POST /admin/fault {stale}` at 14:37:50 → waited real wall-clock time → status crossed to `STALE` at age=48.7s, `last_successful_fetch` fixed at 14:37:49.8, replay frozen at step 18 → digest during the freeze showed `freshness: STALE`, the acked flag correctly absent (no phantom resurfacing), no new flags → `POST /admin/fault {recover}` at 14:38:57 → waited for the scheduler's own next ticks (not forced) → status naturally returned to `LIVE`.
  - **Severity-escalation-flip fired from the live scheduler, not a manual script**, using `seed_demo_escalation_precondition.py`: seeded POWERGRID.NS / 2026-02-16 / `price_zscore` at an artificial `notable` (rank 1) and acked it, after the script independently verified (via the real, unmodified scoring functions) that the true outcome for that real day was `extreme` (rank 3, z=4.121). Froze replay at step 109, seeded+acked, recovered, waited ~20s for the scheduler to naturally advance to step 116 (past the seeded day at step 112) — confirmed via direct DB query the flag was corrected to `severity=extreme, z_score=4.121` (exactly matching the pre-verified real value) and its `flag_ack` row was gone; confirmed via `GET /digest` it reappeared unacknowledged with the correct severity. Cleaned up all demo-created watchlist/flag rows afterward; real Phase 1-3 seeded data (34 tickers, 8567 real_historical price_ticks) confirmed untouched. Note: letting the live scheduler run during this ~15-minute manual verification session organically produced ~1,000 additional genuine flags across the full replay (not fabricated — real scoring of real data); not cleaned up, since they're legitimate system output, not test residue.
  - **Key finding, documented above and in RELIABILITY.md**: genuine same-key severity escalation cannot occur from natural replay resumption alone under this project's daily-close architecture (fixed close + fixed static baseline ⇒ deterministic per trading day). The precondition-seeding script exists specifically to make the *mechanism* demonstrable live within 5 minutes without touching the scoring/upsert/ack-bust code itself.
- Sep 4: Phase 3 correction (pre-commit review) — evidence endpoint's mean/stdev/points now derive from the exact same baseline row as the flag's stored z_score (see architecture note above), instead of an independently-recomputed window; added `test_flagged_point_zscore_is_reproducible_from_response` and updated the existing evidence test to use a real computed baseline rather than an arbitrary z_score. Also changed `POST /ack`'s `ignored` field from bare flag_id ints to `{id, reason}` objects, with a dedicated test. 61 tests total (2 new: reproducibility + ack-ignored-reason), all passing against real Postgres; re-verified real seeded data (34 tickers, 81 flags) untouched throughout.
- Sep 4: Phase 3 (state machine + API surface) implemented — `app/auth.py` (demo-user dependency seam), `app/services/digest.py` (canonical SHA-256 aggregate hash, locked scheme), `app/services/watchlist_access.py` (ownership check), `app/services/evidence.py` ("show your work", real `price_ticks` only), `app/routers/watchlists.py` (`POST /watchlists`, `POST /watchlists/{id}/items`, `GET /watchlists/{id}/digest` with ETag/304, `POST /watchlists/{id}/ack`), `app/routers/tickers.py` (`GET /tickers/{ticker}/evidence`). 22 new tests (7 pure hash unit tests + 15 real-Postgres API integration tests via httpx ASGI transport), 59/59 total passing. Verified against real HTTP (not just ASGI transport) that a 304 response carries the `ETag` header and an empty body. Verified: ack idempotency (no duplicate `flag_ack` row, stable hash on repeat), two-client sequential acks in both orders (neither overwrites the other), cross-watchlist isolation at the real join level, ownership (404 for both non-existent and non-owned watchlists, identically), evidence built from real seeded `price_ticks` with the flag's stored (not recomputed) `z_score`. Found and fixed a real fixture-teardown ordering bug during this work (FK violation deleting a test ticker still referenced by `watchlist_items`) — not a bug in the implementation under test, a test-fixture ordering gap exposed by Phase 3's new watchlist fixtures.
- Sep 4: Phase 2 (core scoring engine) implemented — `app/services/scoring.py` (z-score, severity bands, volume ratio, volatility_regime, sector-relative tag, confidence gate), `app/services/flags.py` (severity-escalation ack-bust upsert, exact transaction from DATA_MODEL.md), `scripts/run_scoring_once.py` (manual trigger, not a scheduler). 19 new tests (13 pure scoring-math unit tests + 4 ack-bust transaction tests against real Postgres + 2 covered above), 37/37 total passing. Ran the manual script for 10 replay steps against Phase 1's real seeded data: 11 `price_zscore` flags and 70 `volatility_regime` flags emitted, all with real `trading_day` values from 2025 (not wall-clock "today"), 0 flags suppressed by the confidence gate (all 34 baselines have `sample_size=30`, none below the 20-day threshold). `TATAMOTORS.NS` correctly has no baseline and produced no flags (absent from Phase 1's seed, as expected).
- Sep 4: Phase 0 scaffolded — backend (FastAPI, asyncpg, raw SQL, requirements.txt), frontend (Next.js/Tailwind via create-next-app), docker-compose (Postgres only), schema migrated via `backend/migrations/001_init.sql` (`psql -f`, no migration tool). `/health` verified locally: real `SELECT 1` round-trip, returns 200.
- Sep 4: Phase 1 (data layer) implemented — fixed ticker universe (`app/data/tickers.py`), historical backfill script (`scripts/seed_historical_data.py`), baseline computation (`app/data/baselines.py`), `HistoricalReplayProvider` + `ReplayClock` (`app/providers/`). 18 tests written and passing against real local Postgres.
- Sep 4: **Real backfill run (locally, outside the dev sandbox where Yahoo was rate-limited) — Phase 1 verified end-to-end against real data.** Results: 34/35 tickers succeeded (`TATAMOTORS.NS` failed — Yahoo 404/no data, correctly absent from `tickers`/`price_ticks`/`baselines`, no fabricated substitute), 8,567 `real_historical` price_ticks (0 duplicates on `(ticker, ts, source)`), 34 baselines all with `sample_size=30` (well above the 20-day confidence gate), `M&M.NS` and `BAJAJ-AUTO.NS` special-character symbols resolved correctly. Replay-provider smoke test (`scripts/smoke_test_replay.py`) run against the real seeded data: emitted ticks used real historical prices in chronological order, all tagged `source='replay_simulated'`. One real-data quirk observed and preserved as-is (not a bug): `M&M.NS` on 2025-09-08 has a repeated close price and `volume=0` as reported by yfinance itself.

## Known bugs / roadblocks
- yfinance/Yahoo Finance was network-blocked (HTTP 429) from the Claude Code dev sandbox during Phase 1 development — resolved by running the seed script locally instead (see Sep 4 backfill entry above). Not a code defect; no fix needed, just noting why the backfill was run outside the sandbox.
- `TATAMOTORS.NS` — Yahoo returns 404/no data for this symbol. Correctly excluded from all tables (no fabricated row anywhere), universe currently effectively 34/35. Worth a periodic retry (yfinance/Yahoo symbol issues are sometimes transient) but not blocking.

## Open decisions still needed
- Exact frontend component structure (Stage 5)
- Which 30-50 tickers make up the fixed universe, and the hand-curated sector buckets
